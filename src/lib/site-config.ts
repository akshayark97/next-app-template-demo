/**
 * Central project branding. Change these when you start a new project — they
 * feed the navbar brand, page metadata/title, the landing-page CTAs, and the
 * email templates, so there's a single place to rebrand.
 *
 * Brand name is intentionally NOT localized (brand names stay constant across
 * languages); translatable copy lives in `messages/<locale>.json`.
 */
export const siteConfig = {
  name: "My App",
  description: "A full-stack Next.js starter template — ready for anything.",
  // Your project's repository / homepage. Powers the landing-page buttons.
  repoUrl: "https://github.com/akshayark97/next-app-template-demo",
} as const;
